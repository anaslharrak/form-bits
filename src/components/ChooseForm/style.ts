import styled from 'styled-components';

export const ChooseFormContainer = styled.div`
    width: 100%;
    height: 100%;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    place-content: center;
    justify-items: center;
    background-color: #2f2f2f;
`;